# ── Non-secret defaults only ──────────────────────────────────────────────────
# Sensitive values (DB_HOST, DB_USER, DB_PASSWORD, etc.) MUST be stored as
# Replit Secrets — never put them here. dotenv does NOT override env vars
# already present in the environment.
PORT=3001
